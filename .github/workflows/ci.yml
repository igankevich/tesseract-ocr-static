---
name: CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Install dependencies
        run: apt-get update -qq && apt-get install -qqy nasm
      - name: Test
        env:
          RUST_BACKTRACE: 1
        run: ./ci/test.sh
